import Link from "next/link";
import { ArrowRight, BookOpenCheck, ClipboardList } from "lucide-react";
import { getPastPapers } from "@/app/actions/exams";

export default async function PastPapersPage() {
  const papers = await getPastPapers();
  return <div className="max-w-7xl mx-auto space-y-7 pb-12"><header><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-emerald-600"><BookOpenCheck className="w-4 h-4" />Native revision library</div><h1 className="text-2xl sm:text-3xl font-black text-slate-900 mt-2">Past Papers &amp; Revision</h1><p className="text-sm text-slate-500 mt-1 max-w-2xl">Work through structured questions directly in LEARN+. Your practice answers are marked with explanations, without downloading a PDF.</p></header><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-5">{papers.map((paper: { id: string; subject: string; year: number; examination_name: string; paper_number: string; level?: string; instructions: string; past_paper_questions?: unknown[]; questions?: unknown[] }) => <article key={paper.id} className="learn-card learn-card-hover p-5"><div className="flex items-start justify-between"><span className="px-2.5 py-1 rounded-full bg-emerald-50 text-emerald-700 text-[10px] font-black">{paper.year}</span><span className="text-xs font-bold text-slate-400">{paper.paper_number}</span></div><h2 className="text-lg font-bold text-slate-900 mt-4">{paper.examination_name}</h2><p className="text-sm font-semibold text-indigo-600 mt-1">{paper.subject} · {paper.level || "National paper"}</p><p className="text-xs text-slate-500 mt-3 line-clamp-2">{paper.instructions}</p><div className="flex items-center justify-between mt-5 pt-4 border-t border-slate-100"><span className="inline-flex items-center gap-1.5 text-xs text-slate-500"><ClipboardList className="w-3.5 h-3.5" />{(paper.questions || paper.past_paper_questions || []).length} questions</span><Link href={`/learner/past-papers/${paper.id}`} className="inline-flex items-center gap-1.5 px-3 py-2 rounded-xl bg-emerald-600 text-white text-xs font-bold">Open paper<ArrowRight className="w-3.5 h-3.5" /></Link></div></article>)}</div>{papers.length === 0 && <div className="learn-card p-10 text-center text-sm text-slate-500">No published past papers are available yet.</div>}</div>;
}
